<form method="post" action="{{ path('comment_delete', {'id': comment.id}) }}" onsubmit="return confirm('une fois le message envoyé, vous ne pouvez pas le modifier.');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ comment.id) }}">
    <button class="btn">Delete</button>
</form>
